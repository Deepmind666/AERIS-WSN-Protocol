AERIS 协议稿件深入评审与提升建议
基于Sensors期刊标准的全面技术评估

结构完整性：稿件结构较为完善，涵盖了引言、理论分析、协议设计、实验结果、讨论和结论等部分，基本符合MDPI Sensors期刊对研究性论文的要求

。引言阐述了WSN可靠性问题的背景，理论分析部分提供了Lyapunov优化等数学支撑，协议设计章节详细描述了AERIS的关键机制（如网关中继、上下文自适应切换等），实验部分进行多角度评估，讨论部分提及局限与未来工作（如节点移动场景）。整体来看，各章节衔接合理，研究方法和结果呈现较完整。不过，可以考虑增设相关工作/文献综述部分，将传统WSN路由协议和AI驱动的新兴方法综述梳理，更清晰地定位本文创新点于现有研究中的位置。此外，材料与方法部分标题可更加标准化（如将第2、4章内容归为“方法”），以匹配期刊惯例

。

创新性：稿件提出了AERIS网关增强路由协议，通过“环境感知的中继选择”和“阈值驱动的上下文切换”来提高WSN可靠性，这在经典LEACH、PEGASIS等协议基础上有所创新。创新点主要包括：①网关节点选择机制，在簇头与基站之间增设中继，以提高长距离链路的可靠交付率；②安全阈值协作机制（Cooperative ARQ及Safety模式），在直接传输失败时采用邻居簇头协助重传，确保近100%传包率；③环境上下文自适应切换（CAS）模块，根据环境因子（如温度）与链路质量的关联实时调整路由模式。以上创新相对于传统协议在现实信道下的可靠性提升显著：实验结果显示AERIS在100–500节点规模下均达到100%数据交付率，而LEACH等经典协议PDR严重下降。特别是安全机制对可靠性的贡献高达+14.6个百分点，是主要贡献来源。总体而言，创新思想清晰且有实证支撑。需要注意的是，CAS上下文切换模块在消融实验中提升有限（对PDR改善统计不显著，p > 0.75）。因此作者应阐明该模块的作用（例如可能在能耗或延迟方面的影响），以突出其存在价值。如果CAS对主要性能指标贡献较小，建议进一步优化算法或强调其在特定场景下的意义，确保创新点实至名归。

理论支撑：作者在第2章提供了理论分析，包括对分布式最优解的失效（Curse of Distributed Optimality）的探讨，以及采用Lyapunov漂移加罚函数框架对协议稳定性进行分析。这种理论推导为AERIS提供了一定的学理支撑。例如，作者定义了虚拟队列衡量能量不均衡，利用Lyapunov函数证明了在拓扑动态受限条件下AERIS性能的有界最优性。这些推导体现出作者对协议设计 深层原理 的把握，有助于说服审稿人稿件具有坚实理论基础。同时，稿件将理论分析结论与实际设计相对应（如证明在节点扰动下的稳定性，为采用阈值策略提供依据），实现了理论与实践的结合。需要确认推导中的假设合理且引用了权威来源，譬如Lyapunov优化在WSN中的应用文献。如果可能，简化理论部分的表述以服务于工程直觉，让读者明了理论结论如何指导AERIS设计，将进一步提升论文说服力。

结果可信性：实验结果具有较高可信度。首先，作者采用Log-Normal Shadowing信道模型(σ = 8 dB)模拟真实环境中的多径衰落，比传统理想信道更具可信度。其次，所有协议的对比均在相同条件下进行，并重复30次独立实验，计算置信区间，这保证了统计显著性。作者执行了Shapiro-Wilk正态性检验、Levene方差齐性检验及Welch t检验并进行了Holm-Bonferroni多重校正，严格验证了差异显著性。例如，AERIS相较LEACH、HEED的PDR提升均通过了极显著性检验(p≈1e-27)。另外还计算了效应量Cohen’s d来评估实际影响程度。这些做法均符合传感器网络论文中数据可靠性和统计严谨的高标准。实验数据和源代码公开获取也增强了结果的可验证性。总的来说，结果可信度高。建议确保在正式稿中清晰报告关键数值和显著性，以满足Sensors对于结果透明的要求。如果可能，可加入对负面结果或失败情况的说明，以体现研究的客观和全面。

实验设计合理性：实验设计总体合理，考虑了多种对比和情景，但仍有可提升之处。基准协议选择方面，作者对比了经典的LEACH、PEGASIS、HEED以及SEP协议，在100节点小规模下还与Q-Learning智能路由算法相比。这些基准具有代表性，涵盖了簇状、链式、自适应等不同范式，使比较具有公平性和代表性。评估指标上，不仅包括PDR和能耗，还考察了网络生存期、可扩展性、可靠性-能量折中等，多维度体现方案性能。同时进行了参数敏感性分析（如安全阈值、Gateway数量等）和消融实验（去除各模块对性能的影响），这些都使实验设计更加完善。需要指出的是，目前实验均在静态拓扑下进行，节点位置固定且无故障。稿件在局限性中也承认尚未验证移动或动态场景。鉴于Sensors关注物联网应用的真实动态性，建议作者在今后工作中补充节点移动、随机故障等动态环境下的性能，或在讨论中强调AERIS设计对此类情景的适应性预期。另一个方面，稿件未明确说明仿真所使用的平台或工具。为了提高实验的公信力，最好在正文交代采用了何种仿真器或环境。如果尚未使用主流仿真平台，建议考虑NS-3、OMNeT++ 或 MATLAB等权威WSN仿真工具来实现实验

。这些平台在WSN研究中应用广泛且经过验证，有助于增强审稿人对结果的信任度

。若作者使用的是自开发仿真代码，应在论文中加以说明并引用类似工具的文献作为支持，表明其合理性和可重复性。

算法适配性：AERIS协议在设计上注重了适配WSN资源受限环境的需求，也具备一定的扩展性。首先，协议关键机制（如网关中继选择和协作ARQ重传）均为局部决策，时间复杂度为O(1)，适合在分布式传感节点上实时执行。这体现了对节点计算和通信能力限制的考虑。不过，作者测得总决策延迟约167 ms，已超出典型微控制器25 ms处理时限，需要借助边缘网关来承担部分计算。这一点在结论中已有提及，但建议在方法部分进一步解释算法的实时性瓶颈和解决方案（如引入更高性能节点或简化算法逻辑）。其次，算法采用阈值和线性判别等简单决策，保证了在不同网络规模下的可扩展性和稳定性。实验显示AERIS在500节点时仍保持100%传输成功，而LEACH等显著下降，证明了算法在更大规模下的适配性和优势。第三，AERIS引入上下文感知模式（正常、激进、保守三种模式）以适应环境变化。尽管当前实现假定静态环境，但这一设计为将来扩展到动态环境（如外界干扰剧烈波动或节点移动）提供了机制储备。总体而言，AERIS算法在能耗-可靠性权衡方面做出了有益探索：牺牲一定能耗实现了可靠性极大化。在后续研究中，作者可尝试让算法适应更多复杂情境，例如拓扑频繁变化或异构节点存在，以检验其普适性。另外，鉴于CAS模块目前对主要指标贡献不大，可考虑改进其自适应策略（例如更高级的预测模型）以提升对环境动态的适应能力。

稿件对创新点、实验方案、图表和AI集成方向的体现

创新点清晰度：当前稿件较充分地体现了环境感知中继和上下文切换机制这两大创新方向。摘要和结论中明确强调了网关增强可靠性（Gateway选择提高+9.4%PDR）和安全机制贡献突出（+14.6%PDR）。上下文自适应切换（CAS）作为创新点也在方法中有所描述，例如根据环境要素切换路由模式。然而，从结果看CAS对提升可靠性的直接作用不明显。为突出这一创新点，作者应在讨论中解释CAS的潜在收益（如减少极端情况下能量消耗或延迟），或提供补充实验展示CAS在特定场景下的效果。总的来说，稿件已经点明了主要创新，但可通过更充分的数据支撑和论述使创新点的价值更加清晰可信。

实验实现平台：论文中未明确指出实验所基于的仿真平台，这可能让审稿人质疑结果的权威性。Sensors等期刊偏好作者使用业界认可的仿真器或测试平台进行实验，例如NS-3、OMNeT++、MATLAB仿真库，或在真实硬件测试床上验证

。这些平台提供了大量现成模型，可减少人为错误并方便同行复现。建议作者在文中补充说明实验的实现方式。如果已经使用了上述平台之一，应明确提及并引用其文档；如果使用自研模拟器，应解释其可信度（例如基于标准模型，结果与文献一致等）。强调权威平台的使用有助于增强实验的说服力，让读者相信结果并非由特殊实现产生偏差。

图表信息承载力：稿件中的图表设计信息量丰富，具有较强的信息承载力。例如，图3将12个子图合并呈现，综合展示了不同协议的PDR、能耗、可扩展性、敏感性分析和消融实验结果于一体。这种多子图合集形式有效传达了大量信息，便于读者纵览全局。然而，需要确保每个子图内容清晰可读、标注充分，避免因版面拥挤而混淆。建议：可以对相关性高的子图进行合并，如将同一组实验的不同指标并排比较，提高图表的对比效果；同时确保统一配色和图例以方便辨识。如果篇幅允许，也可考虑将部分子图拆分成独立图以放大细节，平衡信息量与清晰度。例如，图3(a)–(f)已涵盖主要结果，图3(g)–(l)涉及统计分析和敏感性，可根据需要拆分或合并。总之，图表应突出重点、简洁美观，充分发挥其说明数据的作用。作者在修改时应自查图表是否还存在优化空间，例如提升分辨率、规范单位标记、合并同类项等，使其更符合Sensors期刊对图表专业美观的要求。

AI机制集成潜力：稿件主题侧重于规则阈值算法提升可靠性，但亦涉及AI相关内容的讨论（引用了智能路由算法和AI综述）。目前AERIS采用的上下文切换为线性判别决策，可靠性机制为固定阈值+重传次数，并未深度结合机器学习或智能优化。鉴于近年WSN领域兴起了大量AI驱动的自适应路由方案，作者可考虑适当引入AI机制以增强协议决策逻辑：例如，加入一个基于LSTM的链路质量预测器，利用历史环境数据预测未来链路状态，从而提前调整路由；又如利用图神经网络(GNN)挖掘网络拓扑特征，辅助选择最优网关或簇头；再如应用强化学习(RL)动态优化ARQ重传策略，使协议在不同流量/干扰条件下自适应调整重传次数。引入这些AI技术有望进一步提升AERIS在复杂场景下的性能和智能化程度。然而，需要注意的是，AI模型的引入必须基于充分的特征和数据支撑，并经过实验验证其有效性，否则可能流于空泛。若作者具备相关数据和开发条件，在修稿时可尝试集成一个轻量级的智能模块（例如训练一个简单的决策树或在线学习算法调整阈值），并将其性能与当前策略比较，以证明AI的增益。同时应讨论其开销和可行性，确保不违背WSN能耗约束。如果由于篇幅或时间限制无法在本稿中实现完整的AI集成，建议在结论或未来工作中明确指出这一方向，强调AERIS框架的开放性，可与AI方法结合以进一步提高决策质量。这将向审稿人表明作者对前沿方法有所关注，并为论文增添前瞻性。

GPT-5.2 & Claude 4.5 高性能提示词设计

下面提供一组针对GPT-5.2 Codex和Claude 4.5 Opus模型的高性能Markdown提示词，用于自动生成或增强特定内容模块。每个提示词采用结构化嵌套格式编写，语义标签清晰，便于工程化解析。

prompts:
  - module: "CoreAlgorithmPseudoCode"
    description: "生成AERIS核心算法（网关选择、协作ARQ、簇结构自适应）的伪代码模块。要求分步骤展现算法逻辑，并包含适当的注释解释。"
    prompt: |
      **Task**: Provide pseudocode for the core algorithms of the AERIS protocol in a clear, modular format. The pseudocode should cover:
      1. Gateway selection mechanism (choosing a reliable intermediate node between cluster head and base station).
      2. Cooperative ARQ retransmission strategy (primary transmission with retries, and cooperative fallback with neighboring cluster heads).
      3. Cluster structure adaptation (context-aware switching between Normal, Aggressive, Conservative modes based on environment).
      **Requirements**:
      - Use consistent pseudocode style (e.g., algorithmic steps or Python-like syntax in Markdown format).
      - Clearly label each module (GatewaySelection, CooperativeARQ, ClusterAdaptation) and outline its steps.
      - Include inline comments (`# ...`) to explain key decisions and thresholds.
      - Ensure the logic reflects the O(1) cluster-local decision approach of AERIS and uses placeholders for any input parameters (e.g., link quality metrics, energy levels).
      **Output Format**: 
      Provide a Markdown-formatted pseudocode with each algorithm module presented separately, each preceded by a descriptive comment header.
  
  - module: "HighFidelityFigureGeneration"
    description: "生成高保真实验图形的代码示例，例如包含12个子图矩阵比较不同拓扑/网络条件的图表。要求使用Matplotlib等工具绘制，注释清晰，风格统一。"
    prompt: |
      **Task**: Generate example code to create a high-quality figure with multiple subplots (e.g., a 3x4 grid of 12 plots) for WSN performance comparison under various conditions.
      **Figure Details**:
      - Subplot matrix: 3 rows x 4 columns.
      - Each subplot represents a different scenario or metric (e.g., PDR vs rounds for different protocols, Energy consumption vs node count, Network lifetime curves, etc.).
      - Include titles/subtitles for each subplot indicating the scenario/metric.
      - Share common X and Y axes labels where appropriate (e.g., X-axis could be "Rounds" or "Node Count", Y-axis "PDR (%)" or "Energy (J)").
      - Ensure legends do not overlap with data.
      **Requirements**:
      - Use Python `matplotlib` (or a similar library) in the code.
      - Define dummy data arrays for illustration (since actual data is not provided), ensuring the code is runnable.
      - Apply a consistent style across subplots (e.g., line styles, markers, colors chosen from a defined palette).
      - Annotate code with comments explaining each step (subplot creation, layout adjustments, labeling).
      - Set figure size and resolution to be suitable for publication (e.g., `figsize=(12,8)`, `dpi=300`).
      **Output Format**:
      Provide the Python code inside a Markdown code block, and include a brief Markdown snippet outside the code block describing what the figure represents.
  
  - module: "MultiScenarioNetworkSimulationCode"
    description: "生成多场景动态网络仿真模块的代码框架，基于NS-3或OMNeT++实现，支持节点故障、动态拓扑和多环境参数。要求模块化设计，参数可配置。"
    prompt: |
      **Task**: Outline a code framework for a multi-scenario wireless sensor network simulation using NS-3 (C++ or Python bindings) or OMNeT++.
      The simulation should support:
      - Dynamic topology changes (nodes can move or join/leave the network).
      - Node failures and recoveries.
      - Multiple environment conditions (e.g., different channel models, interference levels).
      **Requirements**:
      - Structure the code in modules/functions (initialization, configuration, run, logging).
      - Include pseudo-code or simplified code for key parts: setting up node mobility, scheduling failures, applying a channel propagation model (e.g., log-normal shadowing).
      - Parameterize the simulation (number of nodes, area size, mobility model, failure rate, etc.) at the top for easy configuration.
      - Provide comments explaining each section of the code and how to adapt it for different scenarios.
      - Use placeholders for any complex implementations (e.g., "/* ... */" for NS-3 specific configurations or a high-level description if not using actual API calls).
      **Output Format**:
      Present the structured pseudo-code or code skeleton in a Markdown code block, using syntax highlighting for C++ (if NS-3) or OMNeT++ ini files as appropriate. Precede it with a short Markdown explanation of the simulation's capabilities.
  
  - module: "AdaptiveAIIntegrationModule"
    description: "生成自适应AI集成模块的提示词，例如基于LSTM的链路质量预测、GNN用于簇头选择、强化学习优化重传策略的设计方案。要求明确输入输出和模型作用。"
    prompt: |
      **Task**: Propose a design for integrating an AI module into the AERIS protocol to enhance decision-making.
      Consider one of the following examples (or a combination):
      1. LSTM-based link quality predictor for proactive route switching.
      2. Graph Neural Network (GNN) for optimal cluster-head (or gateway) selection given network topology and node features.
      3. Reinforcement Learning (Q-learning/Deep RL) for adaptive ARQ retransmission strategy.
      **Design Requirements**:
      - Clearly specify the chosen AI technique and its role in the protocol (what decision it improves, e.g., predicting link outages, selecting next hop, etc.).
      - Outline the input features the model will use (e.g., historical packet loss rates, neighbor count, node energy, environmental readings).
      - Describe the output of the AI module (e.g., a predicted link quality score, a recommended action like "switch to aggressive mode", or an optimal number of retries).
      - Ensure the solution is lightweight enough for WSN context or involves an edge computing element if needed (acknowledge computation limits).
      - Provide the design in a step-by-step or bullet form inside a code block, as a structured plan or pseudo-code (not full code, but algorithmic steps or configuration).
      - Use comments to clarify how the AI model integrates with existing AERIS steps (e.g., where in the protocol loop it is invoked).
      **Output Format**:
      Deliver a Markdown code block with a structured algorithmic description (using pseudo-code or formatted steps) of the AI integration, and include explanatory comments. Precede the code block with a brief introduction describing the goal of this AI module in human-readable terms.


上述提示词采用YAML格式列出了每个模块的名称、描述和具体prompt内容，便于日后扩展和自动处理。在实际使用时，可将其中的prompt文本投入GPT-5.2或Claude 4.5模型，以生成对应的代码或伪代码输出。通过这种模块化提示设计，研发人员可以高效获得所需内容，用于丰富和强化论文的算法描述、实验图表和仿真实现。