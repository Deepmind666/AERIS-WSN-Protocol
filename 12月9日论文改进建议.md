This document provides a suite of specialized prompts for GPT-5.1 Codex to incrementally rewrite and enhance the LaTeX manuscript "Environment-Adaptive Skeleton Routing for Real-World Wireless Sensor Networks" (intended for MDPI Sensors). Each prompt targets a specific improvement area, with clear instructions and context to control the model's output. The prompts ensure that the AI only uses the original text (no invented content) and adds TODO: comments where additional information or manual edits are needed. The output generated by Codex using these prompts will be well-structured LaTeX (complete sections, code environments, figure blocks, etc.) that can be directly integrated into the Sensors LaTeX template.

Usage Note: For each prompt, replace the placeholder or insert the relevant original text from your manuscript (e.g., algorithm code, section text, figure LaTeX) where indicated before feeding it to GPT-5.1 Codex. The model’s output should then be reviewed and any TODO: placeholders should be filled in by the author with the correct information.
Prompt 1: Algorithm Pseudocode Optimization

Goal: Refine the algorithm section’s pseudocode for clarity, completeness, and implementability on embedded devices. This prompt guides Codex to restructure the given algorithm in a cleaner pseudo-code format (using LaTeX's algorithmic or algorithm environment), add commentary on complexity, and ensure the logic is easy to follow. It emphasizes preserving the original algorithm semantics while making the structure clear (e.g., proper indentation, loop/condition notation) and highlighting aspects relevant to microcontroller implementation. Any missing details or values should be marked with TODO rather than guessed.

Context & Instructions: The user will provide the original algorithm pseudocode or description (in LaTeX) from the paper. The model should output an improved version in a proper algorithm environment with a descriptive caption. Key improvements include: adding inline comments or footnotes indicating time complexity for key steps, clarifying any ambiguous operations, and using consistent naming. The prompt explicitly forbids introducing new algorithm steps or altering the logic beyond the original content. If certain parameters or steps are not fully explained in the original, the model should insert a TODO: comment as a placeholder for the author to fill in later. Also, ensure all LaTeX environments are properly closed to avoid compilation issues.

Prompt Template:

You are a LaTeX editor AI specialized in algorithm formatting. Rewrite the following algorithm section in a clearer, well-structured pseudocode format, suitable for MDPI Sensors:

- Use a proper LaTeX algorithm environment with `\begin{algorithm}` and `\begin{algorithmic}` (or similar) and add a concise caption.
- Preserve the algorithm’s original logic and variable names (do **not** invent new steps or variables).
- Structure the pseudocode with proper indentation and keywords (e.g., `\IF ... \ENDIF`, `\FOR ... \ENDFOR`) for clarity.
- Add comments in the pseudocode (using `//`) to indicate the purpose of key steps and, if possible, their time complexity (e.g., // O(n) for a loop).
- Ensure the pseudocode is implementable on microcontroller-class hardware (e.g., avoid pseudo-code that implies unlimited resources).
- If any detail (parameter, condition, etc.) is unclear or missing from the original text, insert a `TODO:` comment with guidance (e.g., `// TODO: specify threshold value`).
- Keep the output self-contained in LaTeX (including `\caption{...}` and label if needed), with no external context required.
- **Do not remove any citation references** if they appear in the algorithm description. Preserve them as is in the appropriate places.
- Make sure the LaTeX syntax is correct and environments are properly closed.

**Original Algorithm Section:**
[Insert the original algorithm LaTeX code or description here]

**Rewritten Algorithm (Optimized):**


After running this prompt with the original algorithm input, the model’s output should be a fully formatted LaTeX algorithm environment with improved pseudocode and comments, ready to paste into the manuscript (with TODO notes for any missing pieces).

Prompt 2: Experimental Section Enhancement

Goal: Enrich and refine the experimental results section by incorporating additional scenarios and analyses. This prompt asks Codex to expand the provided experimental evaluation text with coverage of dynamic or challenging scenarios, edge cases, and deeper analysis of the protocol’s robustness. It guides the model to include placeholders for new content such as moving nodes, link failures, parameter sensitivity tests, and fairness metrics, without fabricating actual results.

Context & Instructions: The user supplies the original text of the Experiments/Results section. The model should rewrite and extend this text to address several specific points:

Dynamic scenarios: Acknowledge experiments in dynamic environments (e.g., changing network topology, moving sink or nodes) and how the algorithm performs there.

Exception cases: Discuss how the approach handles failures or abnormal conditions (node dropout, link outages, etc.).

Robustness analysis: Include commentary on the reliability and stability of results across multiple runs or varied conditions.

Parameter sensitivity: Mention any evaluation of how sensitive the results are to key parameter choices (with TODO if such data needs to be added).

Additional metrics: Introduce metrics like energy fairness or load distribution if not already covered, with TODO notes to insert corresponding results or figures if available.

Throughout, maintain a factual basis — only refer to scenarios or metrics that are either already in the original text or logically follow from it. Do not invent numeric results; use placeholders where new results are needed.

The prompt ensures the model preserves the original results and comparisons, and simply appends or integrates the new discussions. It should also keep the writing in a clear, Sensors-style manner (no overly dense descriptions, and each new aspect clearly introduced). All existing citations should be kept intact. New references (if needed for newly mentioned metrics or scenarios) can be indicated as TODO: cite [X] for the author to fill in.

Prompt Template:

You are an AI assistant improving the experimental results section of a research paper. Enhance the section with additional analyses **based only on the context provided** (do not fabricate data):

- Begin by preserving the original result descriptions and their outcomes.
- Integrate discussions of **dynamic scenarios** (e.g., mobility of nodes or sink, time-varying conditions) and how the proposed method performs under those conditions. *(If the original text doesn’t include this, add a brief placeholder discussion with a `TODO:` for the author to fill specific results.)*
- Add a paragraph on **exceptional cases or failure scenarios** (node failures, packet losses, etc.), describing how the approach maintains performance or what mechanisms handle these cases. Use `TODO:` to mark any quantitative results that should be inserted.
- Include a **robustness analysis**: for example, mention if the results hold over multiple runs or under varying environmental factors. If not originally discussed, insert a general statement and a `TODO:` to include statistical measures (like confidence intervals) if available.
- Discuss **parameter sensitivity**: identify at least one key parameter of the algorithm (e.g., threshold, interval, factor) and note how changing it might affect outcomes. If the original experiments included a sensitivity test, summarize it; if not, add a `TODO:` comment suggesting to include such an analysis.
- Introduce **additional metrics**, such as energy fairness or network load distribution, if relevant. For instance, you might add: "We also evaluated energy consumption fairness across nodes (TODO: include results or figure reference), which indicates …". Only mention metrics that make sense in context; use `TODO:` for actual values or a reference to a new figure/table.
- Ensure the flow is logical: use transitions when adding these new topics (e.g., "Additionally, to assess robustness, ...", "We further consider a scenario where ...").
- **Preserve all original citations and labels** (e.g., figure references like Fig. 5 or citations [21]) as they were. Add citation placeholders with `TODO` for any new literature mentioned.
- Keep the writing style consistent with a scientific report in *Sensors*: clear and concise. Avoid overly long or technical sentences from the original version, favoring clarity.
- Do not introduce any information that isn’t implied by or directly present in the original text, except for the structural additions above (marked with TODO for later completion).

**Original Experiment Section:**
[Insert the original experiment/results section text here]

**Revised Experiment Section (Enhanced):**


Using this prompt will yield an expanded experimental section in LaTeX (multiple paragraphs of text, possibly including figure or table references), with TODO notes indicating where the author should insert specific data or citations. The result should improve the comprehensiveness of the evaluation, covering dynamic and robustness aspects expected by reviewers.

Prompt 3: Language Clarity and Style Polishing

Goal: Rewrite the provided text (any section of the paper) to match the MDPI Sensors style, which is reader-friendly and clear, as opposed to a dense IEEE conference style. This prompt has Codex perform language polishing: simplifying overly complex sentences, clarifying ambiguous phrasing, and ensuring each paragraph starts with a strong topic sentence and flows logically. It will not change technical meaning or introduce new information, and it will retain all domain-specific terms and references.

Context & Instructions: This prompt can be applied to any part of the manuscript (Introduction, Methods, Results, etc.) or the entire draft, one section at a time. The user provides the original text, and the model outputs a refined version. Key guidelines enforced:

High readability: Break up long, complicated sentences. Ensure each sentence conveys one idea clearly. Use the active voice and straightforward language where possible.

Clear expression: Remove redundant phrases and jargon-heavy language. If the original has very compact or shorthand expressions (typical in IEEE style), expand them for clarity. For example, replace "utilizing ML-based approaches yields sub-optimality" with "using machine-learning–based approaches often yields sub-optimal results," if that’s clearer.

Paragraph structure: Ensure each paragraph begins with a clear topic sentence that summarizes the main idea of that paragraph. The following sentences should directly support or elaborate on that idea. Add transitions or connector words between sentences or paragraphs for better flow (e.g., “Furthermore,...”, “As a result,...”, “In contrast,...”).

No information loss: Do not omit any scientific details, quantitative results, or citations. All data and references in the original text must appear in the polished text, unless explicitly marked for removal.

Preserve technical terms and symbols: Do not rephrase standardized terms (e.g., keep "LEACH protocol" as is) and do not alter mathematical notations or variables. If a term is abbreviated, ensure the abbreviation is defined at first use (with a TODO if the definition is missing).

Remove dense/IEEE-style phrasing: For instance, break up sentences that chain multiple clauses with semicolons, and clarify any "respectively"s or "we hereafter do X" if they hinder readability. Convert passive constructs to active voice when appropriate for better readability.

Consistent tense and person: Use consistent past tense for methods and results (if appropriate) or present tense for established facts. Maintain third person or passive voice where required by formal style, but use first person plural (“We”) judiciously if the paper is written in that style (Sensors allows either, so follow the original's approach).

Maintain citation placement: Keep citation numbers [ ] attached to the relevant statement after rewriting. If a sentence is split into two, ensure the citation is placed at the end of the appropriate sentence that still reflects the source.

No fabrication or addition: The model should not insert any new content or claims not present in the original. It should focus purely on rephrasing and clarifying the given text.

Prompt Template:

You are a writing assistant improving the style of an academic text for the MDPI *Sensors* journal. Rewrite the following text to enhance clarity and readability, following these guidelines:

- Use clear and concise language. Split overly long sentences if necessary.
- Ensure each paragraph starts with a strong topic sentence and flows logically.
- Maintain all technical details, data, and citations from the original text.
- Remove any overly dense or convoluted phrasing (e.g., long noun phrases, multiple semicolons), and prefer direct and straightforward expressions.
- Keep terminology consistent and do not alter scientific terms or notations.
- If the original text has grammatical issues or is hard to read, fix it while preserving the original meaning.
- Do **not** introduce any new information or change the meaning of sentences.
- Preserve the placement of existing references (e.g., "[12]"). Do not remove or add citations.
- Use formal academic tone but in a more accessible way (as expected by *Sensors* readers).

**Original Text:**
[Insert the original paragraph(s) or section here]

**Rewritten Text (Polished):**


After applying this prompt, the output will be a cleaned-up version of the input text, still in LaTeX (the model will leave LaTeX commands like equations or citations intact) but with improved readability and flow. The author can then review the changes, ensuring no meaning was unintentionally altered, and accept the polished text into the manuscript.

Prompt 4: Figure and Table Formatting and Caption Improvement

Goal: Standardize figure/table presentation and improve captions so that they are self-contained and informative. This prompt instructs Codex to take a LaTeX figure or table environment from the paper and refine it: unify formatting (size, font, style) if needed, rewrite the caption for clarity and completeness, and suggest additions like statistical significance notation or color scheme consistency. It will also add TODO notes for any required manual adjustments (e.g., to actually change colors or to insert p-values).

Context & Instructions: The user provides the LaTeX code of a figure or table environment, including the \caption{...} and any labels or sizing commands (and possibly a brief description in the text, if applicable). The model’s task is to output a revised version of this environment. Key improvements:

Caption self-containment: The caption should explain what the figure/table shows without requiring readers to read the main text. Ensure all abbreviations or symbols in the figure are defined or described in the caption. For example, if a figure has lines or bars representing certain algorithms, the caption should mention them clearly.

Statistical significance: If the figure compares results (e.g., bar charts of two methods), suggest adding indicators of statistical significance (like * or p-values) for differences, if appropriate. Do not fabricate the actual values—just include a TODO in the caption like “(TODO: add statistical significance markers if applicable)”.

Formatting consistency: Ensure the figure or table uses consistent style as others. For instance, use the same font size for axes across all figures, uniform color palette, and similar line widths. The model can’t modify images directly, but it can add a TODO comment for the author, e.g., % TODO: use a consistent color scheme across figures.

LaTeX specifics: Unify how figures are included. For example, if some figures use \includegraphics[width=\linewidth] and others use absolute dimensions, you might standardize them (or at least point it out in a comment). Ensure each figure has a label and is referenced in the caption if needed (Sensors usually requires “Figure X: ...” style or at least a bold Figure label in caption).

Tables: If the input is a table, ensure the formatting (lines, alignment) matches Sensors style (which often prefers tables without vertical rules and minimal horizontal rules). The caption for tables should be above the table in MDPI style and also self-explanatory.

No content change: Do not alter the actual data displayed in the figure/table. Focus on caption wording and LaTeX presentation. Preserve any references to the figure in the caption text (e.g., if caption says “(see Section 5)” keep it).

If multiple figures are part of one environment (subfigures), ensure sub-captions are labeled clearly (a, b, c) and the main caption summarizes all parts.

Add TODO comments for any remaining stylistic issues that require the author’s action (like adjusting figure resolution or confirming a color choice).

Prompt Template:

You are an AI helping to standardize figure/table formatting and captions in a LaTeX document. Improve the following figure/table environment for clarity and consistency:

- Rewrite the **caption** to be self-contained: describe what is shown and define any acronyms or symbols. The caption should allow understanding the figure without referring to the main text.
- If the figure/table compares multiple methods or scenarios, mention **statistical significance** of differences. For example, note if differences are significant (and how it's indicated, like p<0.05), or add a `TODO` to include significance annotations.
- Ensure formatting commands (e.g., `\includegraphics` size, `\centering`, font sizes in plots) are consistent with a uniform style. Add `TODO` comments if the author should adjust something (e.g., `% TODO: unify this font size with other figures`).
- For color consistency, suggest a unified color scheme (e.g., “TODO: use the same color for Algorithm A across all figures”).
- Keep any labels (`\label{}`) and cross-references intact, and maintain the figure/table numbering.
- Do **not** change the actual data or values in the figure or table. Focus only on presentation.
- If this is a table, ensure the LaTeX tabular format matches MDPI style (minimal borders). For figures, ensure we have `\caption` (below for figures) and it adequately describes all sub-figures if present.
- Add a brief note in the caption or as a comment if the figure is based on data (e.g., “error bars show 95% CI”) for clarity.
- Preserve any existing citations in the caption text, and do not create new ones.

**Original Figure/Table LaTeX:**
[Insert the original figure or table environment here]

**Revised Figure/Table LaTeX:**


Running this prompt will result in a LaTeX block for the figure or table with an improved caption and possibly some commented suggestions (% TODO:). The caption will be more explanatory, and any style inconsistencies will be flagged for the author to fix. The author should then implement the TODO suggestions (like adding significance stars in the actual plot image or adjusting figure styles in the source code) and verify the caption accuracy.

Prompt 5: Introduction & Related Work Expansion

Goal: Update the Introduction and/or Related Work sections to include recent competitor algorithms (from 2023–2025) and clearly differentiate the proposed approach from these works. This prompt guides Codex to weave in mentions of algorithms such as EMRAR, EOAMRCL, HTCBR, etc., highlighting their focus and how they contrast with the paper’s approach. It ensures new references are introduced in context with proper citations (marked as TODO if not provided) and that the flow of the introduction is maintained.

Context & Instructions: The user will supply the text of the Introduction and/or Related Work sections. The model’s output will be a revised version with additional sentences or paragraphs covering the latest relevant works:

Mention recent algorithms (2023–2025): Incorporate references to EMRAR, EOAMRCL, HTCBR, and possibly other state-of-the-art WSN routing methods. For each, give a brief description of what they propose or emphasize. (For example, EMRAR (2024) is an energy-efficient multipath routing algorithm focusing on reliability and accuracy trade-offs
researchgate.net
; EOAMRCL (2024) integrates a Grey Wolf optimizer in a cross-layer design to prolong network lifetime
mdpi.com
; HTCBR (2025) introduces a hybrid trust-based cluster routing to handle heterogeneous traffic and congestion
mdpi.com
.) These descriptions should be concise and factual. If the author has specific references for them, include the citation keys (e.g., “[31]”). Otherwise, use TODO placeholders for citation numbers.

Contextualize differences: Clearly state how the proposed approach (AERIS, presumably) differs from or improves upon these works. For example, “Unlike EMRAR, which requires an offline optimization step, our method... (TODO: ref X)” or “EOAMRCL focuses on MAC/Routing cross-layer optimization, whereas our approach emphasizes environment adaptivity under fixed MAC protocols.” Such statements help position the contribution. Use information from the original introduction to connect these differences.

Maintain logical flow: The introduction should still start broadly about WSN challenges and lead to the gap the paper addresses. The new content about recent works should fit into either the motivation (e.g., “Recent studies have attempted to improve X...”) or the related work paragraph. Use transitional phrases like “Recently,”, “However, ...”, “In contrast, ...” to smoothly integrate comparisons.

No fabrication of specific results of those works: Do not claim something about EMRAR/EOAMRCL/HTCBR that isn't known. Keep it general if specifics aren’t provided (e.g., you can say what their main technique is, but not exact performance numbers unless given). If unsure, add a TODO for the author to insert a detail or correct info.

Citation format: Since this is likely part of the introduction or related work, references should be numeric. If you introduce a new reference, number it as [@] or similar placeholder if the actual number is unknown. The author will replace it with the correct number in the bibliography. Mark it as TODO if needed (for instance, EMRAR [TODO: cite]).

Ensure the tone and style remain consistent with the rest of the paper: formal and academic. Avoid first-person here if the rest of introduction is in third-person, or vice versa.

Keep original content: Do not remove existing sentences unless absolutely necessary for flow; instead, augment around them. Preserve any existing references and ensure any new text does not duplicate what’s already said.

Prompt Template:

You are an expert assistant updating the Introduction/Related Work with recent developments. Revise the text below to include comparisons with competitor algorithms from 2023–2025, and highlight how the proposed work differs:

- Insert mentions of **recent WSN routing algorithms** such as EMRAR (2024), EOAMRCL (2024), HTCBR (2025), etc., with a brief description of each one's approach or novelty. For example, mention if they use multipath routing, cross-layer optimization, trust mechanisms, etc. (Use placeholders like [X] for citations if you don’t have the actual reference number, and add `TODO` for the author to fill in.)
- Clearly articulate the **difference or gap** between these works and our approach. For instance, if our method is fully algorithmic and environment-adaptive, note how that contrasts with those methods (e.g., reliance on learning algorithms, specific assumptions, etc.).
- Keep the narrative flow: the paragraph should transition smoothly into these new additions (e.g., "Recent studies have proposed... However, these do not address ... which our approach tackles by ...").
- Do not introduce any specific data from those papers (no numbers or claims that aren’t given). Stick to general terms like “improved network lifetime” or “addressing mobility” if relevant and known.
- Maintain a formal tone and **preserve existing references**. Add new citation placeholders for the new works, marked as `TODO` if the reference details need to be added.
- Ensure the introduction still leads logically to the statement of our contributions after adding the new comparisons.
- If the Related Work section is separate, you may create a new paragraph there for these recent works, or integrate with existing ones as appropriate.
- Add a sentence explicitly stating how our work (AERIS) fills the gap or is novel relative to all these (this could be an updated contribution statement or at the end of related work).
- Use clear and direct language (Sensors style) and avoid overly long sentences when adding new information.

**Original Introduction/Related Work:**
[Insert the original Introduction and/or Related Work text here]

**Revised Introduction/Related Work (with new competitor refs):**


After applying this prompt, the output text will include new sentences referencing EMRAR, EOAMRCL, HTCBR, etc., seamlessly integrated into the introduction or related work section. The author should then replace TODO citation placeholders with actual reference numbers (ensuring those references are added to the bibliography) and verify that the descriptions of each competitor algorithm are accurate according to the cited papers.

Prompt 6: Conclusion Reinforcement

Goal: Strengthen the conclusion by explicitly acknowledging limitations, outlining future work, and clarifying the applicability of the proposed method. This prompt instructs Codex to take the original Conclusion section and enhance it with a candid discussion of the approach’s limitations and the scope for future improvements, as well as emphasizing the contexts where the approach is most suitable (or not suitable).

Context & Instructions: The user will input the original conclusion text. The model should output a revised conclusion with the following enhancements:

Limitations: Add 1-2 sentences describing the main limitations of the work. These could be inherent assumptions (e.g., fixed sensor nodes, specific hardware, simplified models) or performance trade-offs (e.g., higher energy use in certain cases). If the original conclusion lacks this, the model should include a general statement like “This study is limited by ... (TODO: fill specific limitation).”

Future work: Suggest plausible future research directions or improvements, building on the limitations or new questions. This might include extending the approach to other environments, incorporating machine learning if that was avoided, testing on hardware, scaling to larger networks, etc. Ensure these suggestions align with what was discussed (or not done) in the paper. Use forward-looking language (“Future work could explore...”, “In the future, X may be investigated...”). Each suggestion can be one sentence, or a combined few sentences.

Applicability Boundaries: Clearly state the context in which the proposed solution works best, and where it might not be applicable. For example, “Our approach is best suited for indoor static WSN deployments; its performance in highly mobile or outdoor environments with different wireless standards remains untested (TODO: possible evaluation in future).” This shows awareness of the scope and prevents over-generalization.

Reinforce main findings: While adding the above, keep a strong opening in the conclusion that reiterates the main achievement of the paper (this is often already present: e.g., “In conclusion, we presented ... which achieved ...”). The new content should come after this recap of contributions.

Conciseness and tone: The conclusion should remain concise. Typically, one paragraph (or two short paragraphs) suffice even after adding these parts. The tone should be positive about the contributions but honest about limitations.

No new citations usually in conclusions, unless referring to future work ideas that come from other studies. Generally, avoid adding citations here; focus on the paper’s content. If absolutely needed, mark with TODO for the author to insert a reference.

Preserve original text: Do not remove any concluding statements that are important; rather, append or modify gently to include the new elements. Keep any existing reference to figures or data (though usually conclusions don't have new data).

Prompt Template:

You are an AI assistant improving the Conclusion section of an academic paper. Enhance the conclusion by adding a brief discussion of limitations, future work, and application scope, while preserving the summary of contributions:

- Begin with the original concluding statements (summarize main findings and significance as given).
- Add a sentence or two about the **limitations** of the approach. Be honest but not overly negative. (e.g., “However, this work is limited by ...” or “One limitation of the proposed system is ...”). Use a `TODO` if specific details need author input.
- Follow with **future work** suggestions that directly address these limitations or open new questions. (e.g., “Future studies could explore ...” or “In future work, we plan to ...”). These should align with what’s logical given the study.
- Include a note on the **applicability** or context: clarify where this solution is best applicable, and caution if it may not generalize to certain scenarios (e.g., large-scale outdoor networks, different hardware, etc.), potentially as part of the limitations or a separate sentence.
- Make sure the added content flows naturally. It can be part of the same concluding paragraph or a new paragraph if it gets long.
- Keep the tone confident about what was achieved, but add **self-critical insight** so that readers and reviewers see the authors understand the work’s boundaries.
- Do not introduce any entirely new result or claim. Keep it to interpretation and forward-looking statements.
- Maintain consistency in tense (usually present tense for stating contributions and past tense for what was done).
- No need to add citations in the conclusion; if referring to an idea or existing work for future work, use a `TODO` for a reference if necessary.

**Original Conclusion:**
[Insert the original conclusion text here]

**Revised Conclusion (enhanced):**


Using this prompt will produce a conclusion section that not only wraps up the paper’s contributions but also transparently addresses limitations and future directions. The author should review these additions to ensure they accurately reflect the work’s context (filling any TODO specifics, like particular limitation details) and that they align with any discussion earlier in the paper.

Prompt 7: Citation and Reference Cleanup

Goal: Identify and mark issues in citations and references, such as incomplete entries or placeholder text (e.g., "others.", "et al." usage, missing details), so the author can correct them. This prompt has Codex scan the reference list and citations within the text to ensure they meet MDPI formatting and are fully specified.

Context & Instructions: The user provides the full reference list (the bibliography section in the LaTeX file) and possibly the relevant parts of the text where citations appear if needed. The model will output the same reference list with edits or annotations:

Author names: The reference entries in the provided manuscript may have "others." as a stand-in for multiple authors (e.g., "Lin, C.; others."). MDPI format typically uses “et al.” or lists a certain number of authors then et al. if too many. The model should flag these. For each occurrence of "others." in a reference entry, replace it with et al. or a TODO if the exact authors need to be listed. For example, change "Lin, C.; others." to "Lin, C. et al. (TODO: complete author names)".

Incomplete fields: If there are notes like "Volume/issue/pages to confirm" or "early access" placeholders, convert them into TODO annotations. E.g., replace "Volume/issue/pages to confirm" with "TODO: update volume/issue/pages". If "early access" is noted, ensure to keep it if it’s the only available info but mark if anything is pending (like TODO: add issue/page when available).

Citations in text: If the user also includes parts of the manuscript text, look for phrases like "Zhang et al. [5] and others" or "in [1] and others". These should be corrected to either just the citation or proper wording. Mark these with TODO for the author. For example, if the text says "Zhang [15] and others showed ...", the model can change it to "Zhang et al. [15] showed ..." or add a comment %(TODO: fix citation format) if unsure. However, focus on the reference list primarily, as that’s where "others." likely appears.

Formatting consistency: Ensure each reference has the required components (authors, title, journal, year, volume, pages, DOI). If any component is clearly missing or marked with a note, flag it. Do not attempt to fetch missing data; just annotate.

No reordering or renumbering: Keep the reference numbering the same as given. The model should not assume to renumber references.

LaTeX environment: The references may be in a thebibliography environment or similar. Preserve the environment structure (\begin{thebibliography}{} if present, and \bibitem entries). If the user just provides a list, the model can output the list with numbers as given.

Highlight possible "fabricated" or placeholder references: If any reference looks suspicious (e.g., a reference that might not exist or was a placeholder like “Author, X. Title. others.”), mark it with TODO: for verification. Do not delete it—just point it out.

Use comments or TODO within the .bib entries in a way that won’t break LaTeX compilation (LaTeX will ignore unknown text in a bib entry?), or perhaps as a comment % at end of line. For safety, we can use % TODO: comments at the end of lines needing fix, or within the text if we enclose in a safe manner.

For example: \bibitem{ref8} Zhang, R.; **et al.** Geographical Adaptive Fidelity ... 2010, 9, ... doi:... % TODO: Check author list "others." replaced with et al.

Or simply include the TODO as plain text in bold which the author will notice and remove (since MDPI reference style might not be strict about extra text—better to comment if possible).

Ensure all occurrences of "others." are handled. Also look for "early access; volume/issue/pages pending" and mark those.

Prompt Template:

You are an AI assistant checking the reference list and citations for consistency and completeness. Review the following references and annotate any issues:

- Replace "others." with "et al." in author lists, and add a `TODO` if the full author list needs to be verified or completed. (e.g., "Lin, C.; **et al.** *TODO: verify remaining authors*").
- Identify any placeholder text like "Volume/issue/pages to confirm", "early access", or "pending" in the entries. Mark these with `TODO` for the author to update. (e.g., add "*TODO: update volume/issue when available*").
- Ensure each reference has necessary details (authors, title, source, year, etc.). If any detail is missing, insert a `TODO` note for it.
- In the main text (if provided), spot phrases like "and others" following a citation. Modify them to a proper citation format or add a `TODO` comment for the author to fix. For example, change "Zhang [8] and others" to "Zhang et al. [8]" or comment `%(TODO: clarify "and others")`.
- Do not remove any references or change their order. Just annotate problems.
- Provide the output as an updated reference list in LaTeX format, including the `\bibitem` or numbering as given, with inline `TODO` notes or comments for any issues.
- Make sure the output is still compilable (comments in LaTeX references should be okay). Use `%` to start comments for safety where possible.

**Original References:**
[Insert the reference list from the manuscript here (and optionally any problematic citation usages in the text)]

**Annotated References:**


When this prompt is used, the model will output the reference list with TODO annotations. For example, an entry in the original like:

6. Lin, C.; others. Cluster-Based Architecture for Wireless Sensor Networks. IEEE Communications Magazine 2002, 40, 112–114. Volume/issue/pages to confirm, doi:...


might be output as:

6. Lin, C. **et al.** Cluster-Based Architecture for Wireless Sensor Networks. *IEEE Communications Magazine* **2002**, *40*, 112–114. **TODO: confirm volume/issue/pages**. doi:10.1109/MCOM.2002.1024422.


with appropriate LaTeX markup if within \bibitem. The author should then go through each TODO and fix the reference (e.g., replace with correct author list or remove the TODO after confirming details). The end result will be a cleaned, properly formatted reference list for the final paper.

Each of these prompts is designed to be used independently on the relevant section of the paper. By following the prompt instructions and then performing the manual TODO fixes, the author can systematically improve the manuscript's content quality, clarity, and compliance with Sensors journal standards.