# 数据一致性问题报告

**日期**: 2024-12-31
**严重程度**: 🔴 严重

---

## 发现的问题

### 存在两个不同的消融实验数据集

| 文件 | 样本量 | FULL PDR | -GW PDR | -SAFETY PDR |
|------|--------|----------|---------|-------------|
| `intel_ablation.json` | 50 | 0.4769 | 0.3832 | 0.3686 |
| `intel_ablation_parallel.json` | 200 | 0.8999 | 0.7409 | 0.8908 |

**差异分析**:
- PDR值差异巨大 (0.48 vs 0.90)
- 样本量不同 (50 vs 200)
- 这两个数据集不可能来自同一个实验

### 统计验证文件使用了错误的数据

`results/statistical_validation/comprehensive_validation_results.json` 使用的是 `intel_ablation_parallel.json`，其中的效应量计算结果是：
- FULL vs -GW: g = 10.09 (这就是论文中错误数值的来源！)

而我们修正后使用的 `intel_ablation.json` 计算结果是：
- FULL vs -GW: g = 4.48

---

## 问题根源分析

1. **可能原因1**: 两个实验使用了不同的参数配置
2. **可能原因2**: 两个实验使用了不同的数据集
3. **可能原因3**: 其中一个数据集是错误的或过时的

---

## 需要确认的问题

1. 哪个数据集是正确的？
2. 为什么会有两个不同的消融实验数据？
3. 论文应该使用哪个数据集？

---

## 建议的解决方案

### 方案1: 使用 `intel_ablation.json` (当前选择)

**理由**:
- 数据结构更清晰
- 包含完整的seed列表，可追溯
- PDR值更符合WSN实验的典型范围

**需要做的**:
- 确保论文中所有数值与此数据一致
- 重新生成所有图表
- 更新统计验证结果

### 方案2: 使用 `intel_ablation_parallel.json`

**理由**:
- 样本量更大 (200 vs 50)
- 统计验证已经完成

**问题**:
- PDR值异常高 (0.90)
- 需要解释为什么PDR这么高

---

## 当前状态

- ✅ 论文已修正为使用 `intel_ablation.json` 的数据
- ✅ 效应量已修正为 g=4.48
- ⚠️ 需要确认数据选择是否正确
- ⚠️ 需要重新生成统计验证结果

---

## 行动项

1. [ ] 确认 `intel_ablation.json` 是正确的数据源
2. [ ] 删除或归档 `intel_ablation_parallel.json`
3. [ ] 重新运行统计验证，使用正确的数据
4. [ ] 更新所有相关图表

---

*报告生成: 2024-12-31*
