# 论文修复计划 - 立即执行

**日期**: 2024-12-31
**优先级**: 紧急

---

## 已确认的问题

### 1. 效应量数值错误 ✅ 已验证

**论文中的错误值**:
- Gateway: g = 10.09 ❌
- Fairness: g = 0.59
- Safety: g = 0.42
- CAS: g = 0.00

**正确值** (从真实数据计算):
- Gateway: g = 4.475 ✅
- Safety: g = 3.476 ✅
- Fairness: g ≈ -0.10 (可忽略)
- CAS: g ≈ -0.15 (可忽略)

### 2. 需要修改的位置

在 `for_submission/aeris_paper_final.tex` 中:

1. **摘要** (第45行附近):
   ```
   错误: Hedges' $g=10.09$
   正确: Hedges' $g=4.48$
   ```

2. **Introduction** (第60行附近):
   ```
   错误: Hedges' $g=10.09$
   正确: Hedges' $g=4.48$
   ```

3. **Results - Ablation Study** (第115行附近):
   ```
   错误: $g=10.09$ (Large)
   正确: $g=4.48$ (Large)
   
   错误: $g=0.59$ (Medium)
   正确: $g=-0.10$ (Negligible)
   
   错误: $g=0.42$ (Small)
   正确: $g=3.48$ (Large)
   
   错误: $g=0.00$ (Negligible)
   正确: $g=-0.15$ (Negligible)
   ```

4. **Discussion** (第155行附近):
   ```
   错误: $g=10.09$
   正确: $g=4.48$
   ```

5. **Conclusion** (第175行附近):
   ```
   错误: $g=10.09$
   正确: $g=4.48$
   ```

---

## 修复步骤

### 步骤1: 修正论文中的效应量数值

需要将所有 `g=10.09` 替换为 `g=4.48`

### 步骤2: 更新效应量解释

正确的解释:
- Gateway (g=4.48): **大效应** - 核心创新，移除后PDR下降19.7%
- Safety (g=3.48): **大效应** - 重要机制，移除后PDR下降22.7%
- Fairness (g=-0.10): **可忽略** - 对PDR影响很小
- CAS (g=-0.15): **可忽略** - 框架组件，不直接影响PDR

### 步骤3: 重新生成图表

需要确保图表中的效应量数值与论文一致

---

## 正确的数据摘要

```
消融实验结果 (n=50 per config):
┌──────────┬──────────┬──────────┬──────────┬──────────┐
│ Config   │ PDR Mean │ PDR Std  │ Change   │ Hedges g │
├──────────┼──────────┼──────────┼──────────┼──────────┤
│ FULL     │ 0.4769   │ 0.0245   │ baseline │ -        │
│ -GW      │ 0.3832   │ 0.0162   │ -19.7%   │ 4.475    │
│ -SAFETY  │ 0.3686   │ 0.0362   │ -22.7%   │ 3.476    │
│ -FAIR    │ 0.4792   │ 0.0205   │ +0.5%    │ -0.102   │
│ -CAS     │ 0.4806   │ 0.0225   │ +0.8%    │ -0.153   │
└──────────┴──────────┴──────────┴──────────┴──────────┘
```

---

## 执行清单

- [x] 修正摘要中的效应量 ✅ 已完成
- [x] 修正Introduction中的效应量 ✅ 已完成
- [x] 修正Results中的效应量列表 ✅ 已完成
- [x] 修正Discussion中的效应量 ✅ 已完成
- [x] 修正Conclusion中的效应量 ✅ 已完成
- [ ] 重新生成效应量图表
- [ ] 验证所有数值一致性
- [ ] 重新编译PDF

---

*此文档用于指导论文修复工作*
